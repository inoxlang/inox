manifest {}


# a pattern namespace definition is created by assigning an object or a record to a pattern namespace identifier

pnamespace mynamespace. = {
    user: %{name: str}  
    post: %{title: str}  
}

print(%mynamespace.user)
print(%mynamespace.post)
