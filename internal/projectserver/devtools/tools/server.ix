manifest {
    permissions: {
        provide: APP_LISTENING_ADDR
    }
}

server = http.Server!(APP_LISTENING_ADDR, {
    routing: {
        dynamic: __mod-dir.join(./routes/)
    }
})

server.wait_closed!()
