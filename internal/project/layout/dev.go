package layout

import "github.com/inoxlang/inox/internal/inoxconsts"

const (
	DEV_TOOLS_DIR_PATH           = "/" + inoxconsts.DEV_DIR_NAME + "/tools/"
	DEV_TOOLS_SERVER_ENTRY_POINT = DEV_TOOLS_DIR_PATH + "server.ix"
)
