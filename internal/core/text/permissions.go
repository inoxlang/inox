package text

const (
	//permissions section
	INVALID_COMMANDS_PREFIX = "invalid manifest, use: commands: "
	CMD_NAME_ERROR          = INVALID_COMMANDS_PREFIX + "a command (or subcommand) name should be followed by object literals with the next subcommands as keys (or empty)"
)
